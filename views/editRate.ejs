<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Rental Facility App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Rental Facility App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addRate">Add new rate</a>
              </li> 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Rate</h2></div>
        <br>
        <div class="text-center">
          <% if (rate) { %> 
            <form action="/editRate/<%= rate.rateId %>" method="POST" enctype="multipart/form-data">
                <label for="facility">Facility:</label><br>
                <input type="text" id="facilityid" name="facilityid" value = "<%= rate.facilityid %>" required><br><br>

                <label for="day">Day:</label><br>
                <input type="select" id="week" name="week" required><br><br>
                  <option value="Weekday">Weekday</option>
                  <option value="Weekend">Weekend</option>

                <label for="peak">Peak:</label><br>
                <input type="select" id="peak" name="peak" required><br><br>
                  <option value="peak">peak</option>
                  <option value="non_peak">non_peak</option>

                <label for="price">Price:</label><br>
                <input type="number" id="price" name="price" min="0" step="0.01" required><br><br>

                <button type="submit">Update Rate</button>
            </form>
          <% } else { %>
              <p>No Rate found.</p>
          <% } %>
          <a href ="/rate">Back</a>
        </div>
    </div>
</body>
</html>
