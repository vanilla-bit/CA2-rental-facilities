<!DOCTYPE html>
<html>
<head>
  <title>Users</title>
</head>
<body>
  <h1>Users</h1>
  <a href="/users/add">Add User</a>
  <ul>
    <% users.forEach(user => { %>
      <li><%= user.name %> - <%= user.email %> - <%= user.phone %></li>
    <% }) %>
  </ul>
</body>
</html>
